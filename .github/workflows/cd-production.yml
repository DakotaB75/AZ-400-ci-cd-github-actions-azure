name: CD Production

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy-production:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          echo "Building Docker image for production (simulated)"

      - name: Push image to registry
        run: |
          echo "Pushing image to ACR (simulated)"

      - name: Deploy to Production
        run: |
          echo "Deploying to Azure App Service / AKS (simulated)"

      - name: Production health check
        run: |
          echo "Production health check passed"
