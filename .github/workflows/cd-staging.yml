name: CD Staging

on:
  push:
    branches: [develop]
  workflow_dispatch:

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    environment: staging

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          echo "Building Docker image for staging (simulated)"

      - name: Push image to registry
        run: |
          echo "Pushing image to ACR (simulated)"

      - name: Deploy to Staging
        run: |
          echo "Deploying to Azure App Service - STAGING (simulated)"

      - name: Health check
        run: |
          echo "Staging health check passed"
